diff a/src/main/webapp/JSP_Folder/vote.jsp b/src/main/webapp/JSP_Folder/vote.jsp	(rejected hunks)
@@ -24,12 +24,15 @@
 <body>
 
    <%
-   SqlLink sl = new SqlLink(); // DB 연결
    String id = request.getParameter("id");
    String pw = request.getParameter("pw");
+
+   SqlLink sl = new SqlLink(); // DB 연결
    String name = sl.checkLogin(id, pw);
    int stop = sl.checkVoteCount();
    int voted = sl.isvoted(id, pw);
+   sl.linkDisconnect();
+   
    System.out.println(id + ", " + pw + ", " + name);
    %>
 
